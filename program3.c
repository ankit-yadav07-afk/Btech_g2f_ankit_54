#include <stdio.h>
#include<math.h>
int main()
{
    int a,b;
    printf("enter the value of a&b");
    scanf("%d%d",&a,&b);
    if(a==b)
 {   printf("\nboth are equal");}
 else
 {
    printf("both are not equal");

 }
 return 0;
}